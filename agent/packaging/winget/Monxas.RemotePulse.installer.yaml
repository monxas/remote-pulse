# Installer manifest for Remote-Pulse
# Template placeholders to be replaced by release workflow:
#   {VERSION} - e.g., "0.1.0"
#   {INSTALLER_URL} - e.g., "https://github.com/monxas/remote-pulse/releases/download/v0.1.0/rp-windows-x64.exe"
#   {SHA256} - SHA256 hash of the installer binary

PackageIdentifier: Monxas.RemotePulse
PackageVersion: {VERSION}
Platform:
- Windows.Desktop
MinimumOSVersion: 10.0.0.0
InstallerType: portable
Commands:
- rp
ReleaseDate: {RELEASE_DATE}
Installers:
- Architecture: x64
  InstallerUrl: {INSTALLER_URL}
  InstallerSha256: {SHA256}
ManifestType: installer
ManifestVersion: 1.6.0
